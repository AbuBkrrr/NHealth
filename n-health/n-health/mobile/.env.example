# Point this at your deployed backend (or your machine's LAN IP for local dev
# on a physical device - localhost won't resolve from a phone/emulator).
# Must end in /api.
EXPO_PUBLIC_API_URL=http://localhost:4000/api
